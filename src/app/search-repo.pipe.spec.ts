import { SearchRepoPipe } from './search-repo.pipe';

describe('SearchRepoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRepoPipe();
    expect(pipe).toBeTruthy();
  });
});
